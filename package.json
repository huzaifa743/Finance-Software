{
  "name": "finance-software-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n backend,frontend -c green,blue \"npm --prefix backend run start\" \"npm --prefix frontend run preview\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
