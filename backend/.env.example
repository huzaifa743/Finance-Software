PORT=5000
JWT_SECRET=your-super-secret-jwt-key-change-in-production
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Optional: SQLite database path (set to your Railway volume path in production)
# Example: DB_PATH=/data/finance.db
DB_PATH=

# SMTP (for Email reports from Settings)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password
SMTP_FROM=Finance Reports <your-email@example.com>
